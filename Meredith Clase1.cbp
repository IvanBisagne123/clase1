#include <stdio.h>
#include <stdlib.h>
#define CANTIDAD_REP 3
int PedirSumarMostrar()
{
    float num1;
    float num2;
    float resultado;
    printf("digite numero 1");
    scanf("%f",&num1);
    printf("Digite numero 2");
    scanf("%f",&num2)
    resultado = num1 + num2;
    printf("el resultado es: %.2f",resultado);

    return 0;
}
    int main ()
    {
        if(PedirSumarMostrar()==0)
     {
         printf("\nATR");
     }
        return 0;
    }



//Sacar el promedio//

int PedirAcumularMostrarPromedio()
{
    int acumulado=0;
    int buffer;
    int i;
    for(i=0;i<CANTIDAD_REP;i++)
    {
      printf("in numero1 %d:",i+1);
      scanf("%d", &buffer);
      //falta validar//
      acumulador = acumulador + buffer;
    }

    printf("el promedio es: %f",acumulador/CANTIDAD_REP);
    return 0;

    //BUFFER Y ACUMULADOR SON DOS ENTEROS//
}

